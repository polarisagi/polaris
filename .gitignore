# Binaries
/polaris
/polaris-*
*.exe
*.dll
*.so
*.dylib
/bin/
/dist/

# Go
*.test
*.out
go.work

# Rust
rust/target/

# IDE
.idea/
.vscode/
.cursor
.claude/
.gemini/
*.swp
*.swo
*~

# OS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Config
.env
.env.local
configs/local.yaml

# Data directories (runtime)
/data/
sessions/
/logs/
*.log

# Databases & Embedded Stores
*.db
*.sqlite
*.sqlite3
*-wal
*-shm
*.lancedb

# Backups & Archives
*.tar.zst
*.tar.gz
*.zip
*.bak

# Personal workspace (不入库)
/local_playground/
code_review_report.md
.agents/

# scripts/restart.sh 本地测试用独立数据目录（隔离 launchd 生产实例的 ~/.polarisagi/polaris）
/.devdata/

# Scripts & Misc
__pycache__/
*.py[cod]

# Firecrawl search cache
.firecrawl/

# Web UI — Node.js (根目录误生成的也一并排除)
node_modules/
/package-lock.json
/package.json

# Web UI build artifacts（Go embed 需要目录存在，用 .gitkeep 占位）
web/dist/*
!web/dist/.gitkeep
web/bin/
web/node_modules/

# Skills — Wasm 编译产物（由 make build-skills 生成，不提交）
skills/**/*.wasm

# Worktrees
.worktrees/
worktrees/
.sisyphus/
repomix-output.xml


pkg/swarm/orchestrator/test_fanout_out.csv
internal/execute/orchestrator/test_fanout_out.csv
docs/research/

# ── 私钥防误提交（ADR-0095 决策二）────────────────────────────────────────────
# cosign generate-key-pair 会在当前目录产出 cosign.key（加密私钥）。发布私钥只应
# 存在于 GitHub Secrets，绝不入库。这里是最后一道防线——正确做法是在仓库外的临时
# 目录生成密钥，上传 Secret 后立即安全删除（macOS: rm -P）。
#
# 注意 releasekeys/*.pem 是**公钥**，必须入库（客户端内嵌信任根），故不在此忽略；
# 下面只挡私钥常见文件名。
cosign.key
*.key
!internal/protocol/schema/*.key
signing-key*
