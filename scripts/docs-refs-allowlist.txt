# docs-refs 白名单 —— 活文档中「本就不该存在」的路径引用。
#
# 唯一合法用途：文档正在**记载历史**（"该路径已于某日删除/迁移至 X，见 ADR-XXXX"），
# 路径不存在恰恰是文档正确的证明。除此之外一律修文档，不要往这里加。
#
# 格式：每行一个引用字符串（与文档反引号里的写法逐字一致，含或不含尾部 /）。
# 同行 # 之后为缘由说明，必须写明"为何该路径注定不存在"。
# 校验：make docs-refs（已并入 make check-all）。

# ── ADR-0046（2026-07-12/13）：dag 与 orchestrator 物理迁入 internal/execute ──
# 迁移前路径。M04/M08 文件头与 internal/execute/CLAUDE.md 的迁移注记必须写出旧路径，
# 否则读者无法把历史 commit / 旧 ADR 里的包名对应到现在的位置。
internal/agent/dag
internal/agent/dag/
internal/swarm/orchestrator
internal/swarm/orchestrator/

# ── ADR-0062（2026-07-14）：SwarmRouter / CapabilityRegistry / TopologyEvolver 整包删除 ──
# M08 §3-bis 与 §8 记载"本节曾描述的机制已随该包删除"，路径注定不存在。
internal/swarm/topology/

# ── ADR-0062（2026-07-22）deadcode 清理：event_buffer 已删除 ──
# M02 §1.3 记载该缓冲层已删除、写路径改为直落，需要点名旧文件读者才知道说的是哪个。
internal/store/audit/event_buffer.go

# ── 协议层接口按域拆分：interfaces_vfs.go 未随之产生 ──
# M11 §5.1 记载 VFS 相关接口未单独成文件（并入其它 interfaces_*.go），
# 明写该文件名是为了阻止后来者按"每域一文件"的惯例去找/去建这个文件。
internal/protocol/interfaces_vfs.go

# ── tool_stage.go 彻底清理 ──
# M13 §1.2.7 记载 ContextRefExpander 重构后该文件已不存在，防止重复提出"它去哪了"。
internal/agent/context/tool_stage.go

# ══════════════════════════════════════════════════════════════════════════════
# 以下条目自 2026-08-08 起同时服务 make docs-refs 的 .go 注释扫描
# （tools/comment_refs.go），均为「注释在记载历史」的合法引用。
# ══════════════════════════════════════════════════════════════════════════════

# ── 四层布局迁移（pkg/* → internal/*）前的旧顶层包名 ──
# internal/lint 的多条规则注释、internal/config 的旧规则表引文都必须写出旧包名，
# 否则读者无法把「2026-07-07 修正：原规则表键入 pkg/substrate|cognition|...」这类
# 记录对应到今天的目录。这些顶层名注定不会再出现，任何新引用按定义都是历史注记。
pkg/substrate
pkg/cognition
pkg/action
pkg/gateway
pkg/edge

# ── ADR-0062 deadcode 清理：整包/整文件删除，注释点名旧路径读者才知道说的是谁 ──
internal/tool/builtin/memory_prune
internal/knowledge/ingester.go

# ── 从未存在过的 baseline 幽灵条目（inv_lint_test.go 记录其成因）──
# 2026-07-07「按当前 internal/ 结构手写 baseline」时凭空写出的两条，
# 注释明写「不仅当前不存在，在整个 git 历史里也从未存在过」。
internal/sandbox/wasm_host.go
internal/tool/builtin/sandboxenv/hostenv.go

# ── ADR-0085 会话编排层抽出：文件迁移注记 ──
# internal/gateway/session/transcript.go 文件头写明「本文件自 X 原样迁入」。
internal/gateway/server/chat/transcript.go

# ── CLAUDE.md 历史误写：内部错误包从来不叫 internal/errors ──
# pkg/apperr/apperr_test.go 注释明写该包不存在，用于阻止后来者再去找它。
internal/errors

# ── 不是路径：MCP JSON-RPC 方法名 ──
# internal/extension/mcp 的并发测试注释里的 tools/list 是协议方法名。
tools/list

# ── ADR-0089 决策二：一键重建 baseline 的工具已删除 ──
# inv_BareErrorReturnRatchet 的注释必须点名它，否则读者无从理解「baseline 为空
# 且没有重建入口」是刻意设计而非遗漏。
cmd/gen_bare_error_baseline

# ── Cargo 编译产物路径：在未执行 cargo build 的 CI 环境（如 docs-toc）中不存在 ──
# internal/ffi/dylib_test.go 验证加载路径包含该目录，属于测试断言说明。
rust/substrate/target/release

