# anchor-refs 存量白名单（棘轮模式，ADR-0088 先例）——只许收窄，不许批量新增。
#
# 背景：2026-08-09 P4-0-2（local_playground/prompt/docs-optimization-plan.md）首次
# 引入 § 章节锚点门控（tools/anchor_refs.go），首跑即产生存量——历史注释从未被此类
# 门控扫过。按方案要求：本阶段不清偿存量，只挡新增；已确认判定的条目才可移出本列表。
#
# 每行格式：<模块代号> §<章节号>（与工具报错里的 ref 字段逐字一致）。
#
# 移出本列表前须验证：引用确实已修正为有效章节号，或该引用本就是刻意的非章节命名
# 记法（如 §HITL/§Trace 类词面锚点，非"## N."格式的编号标题）——移出理由写进
# commit message，不得静默清空。
#
# ---- 2026-08-09 存量清零（23 → 0），逐条审计而非批量清空 ----
# 首跑 23 类分属两种性质，处置方式不同，均非"照单加白名单"：
#
# A. 工具误报（标题真实存在，索引器认不出）——修工具，不进白名单，共 5 类：
#    M08 §3-quinquies / §3-sexies、M01 §5.2-bis、M04 §L3、M09 §RAGAS。
#    原 headingRe 只认"纯数字 + 可选 -bis/-ter"，看不见拉丁序数后缀、点号后再带
#    后缀的 N.M-bis、以及命名式标题。M01 §5.2-5.4 这类区间记法同理。
#    把这 5 类写进白名单等于永久掩盖这些文件后续的真实漂移（ADR-0091）。
#
# B. 真实漂移（章节确实改过/引用本就写错）——改引用或改文档，共 18 类：
#    最大两类 M04 §B2（16 处）与 M05 §11.3（17 处），处置方向刻意相反——
#    · M04 §B2：arch 模块编号(M04-Agent-Kernel)与 specs 文件编号(04-Module-Boundary)
#      撞名"04"，这 16 处从来指的是后者，已全部改写为无歧义的完整路径记法；
#    · M05 §11.3：17 处独立引用一致指向同一节，是 M05 标题在某次编辑中丢了编号，
#      故改文档补回编号，不动这 17 处引用（多数独立引用即证据）。
#    其余：M04 §5.3→§4.3、§5.5→§4.5、M11 §5.4→§5.1、M08 §8.2→§5.1、
#    M06 §6.3/§116→§2.2、M10 §4.X→§4.1、§0→§0-bis、M13 §1.X→§1.2.5、
#    §1.x→§1.5、§HITL→§2.4、M13-bis §3.3→§5.3、M05 §ContextZone→§2.1、
#    §1-5→§2-5、M03 §Trace→§9.1。
#
# 清零后本文件保留（而非删除）：门控仍在运行，未来若出现暂不清偿的存量，
# 仍走本文件逐条登记。空列表 = 当前零存量债，不是"门控未启用"。
