--- internal/knowledge/rag_impl_test.go
+++ internal/knowledge/rag_impl_test.go
@@ -101,7 +101,7 @@
 	}
 
 	// Expand chunks
-	expanded, err := expander.Expand(ctx, chunks)
+	expanded, err := expander.Expand(ctx, chunks, types.TaintLow)
 	if err != nil {
 		t.Fatalf("Expand failed: %v", err)
 	}
@@ -127,7 +127,7 @@
 	}
 
 	// Expand chunks
-	expanded, err := expander.Expand(ctx, chunks)
+	expanded, err := expander.Expand(ctx, chunks, types.TaintLow)
 	if err != nil {
 		t.Fatalf("Expand failed: %v", err)
 	}
