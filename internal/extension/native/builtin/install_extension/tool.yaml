name: install_extension
description: "Download and install an extension (plugin, skill, or MCP server) by its catalog ID. Always use search_extension first to find the correct catalog ID. After installation, the new tools or skills become immediately available in this session."
version: "1.0.0"
capability: write-local
risk_level: high
sandbox: in-process
side_effects:
  - network-call
  - file-write
tags:
  - marketplace
  - extensions
  - install
