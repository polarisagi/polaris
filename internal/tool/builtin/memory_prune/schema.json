{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "entity_type": {
      "type": "string",
      "description": "The type of the memory entity to prune (e.g., 'concept', 'preference', 'fact')."
    },
    "entity_name": {
      "type": "string",
      "description": "The exact name or key of the entity to prune."
    },
    "reason": {
      "type": "string",
      "description": "A clear explanation of why this entity is being pruned (e.g., 'Outdated information', 'User requested forget')."
    }
  },
  "required": ["entity_type", "entity_name", "reason"]
}
