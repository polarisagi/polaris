Based on these agent session events, update the user profile. Return ONLY valid JSON with keys: stable_facts (object), recent_activity (string array ≤10 items), behavioral_patterns (object).

Current profile: {{.CurrentJSON}}

New events:
{{.Events}}
